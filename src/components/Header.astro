---
// import Navigation to Header
import Navigation from "./Navigation.astro";
---

<header
    class="bg-slate-950 rounded-sm px-4 py-3 shadow-xl shadow-red-200/50 inset-ring-2 inset-ring-red-800/75"
>
    <nav>
        <!-- Put a logo or something here -->

        <Navigation />

        <!-- Mobile Header -->
        <!-- <Menu /> -->
        <!-- <ThemeIcon /> -->
    </nav>
</header>

<!-- <style>
    header {
        margin: 0;
        padding: 0.5em 1em;
        box-shadow: 0 2px 8px rgba(var(--Void), 5%);
        border-bottom: 3px solid #cb2928;
    }
    /* h2 {
        margin: 0;
        font-size: 1em;
    } */

    /* nav {
        margin-left: 10px;
        display: flex;
        align-items: center;
        justify-content: start;
    }
    nav-links {
        text-decoration: none;
        border: 3px solid #cb2928;
    } */
    /* // Hiding menu @ Screen-size 720px */
    @media (max-width: 720px) {
        .social-links {
            display: none;
        }
    }

    /* Format the nav below screen 720px */
    /* @media (min-width: 350px) (max-width: 719px) {
        header {
            border: 3px dashed var(--Void);
        } */
    /* } */
</style> -->
